**Background**

This is an implementation of a **doubly linked list.** 

**Features**

- We can iterate forwards and backwards
- We can query the size of the list and if it is empty
- Two lists can be compared using `equal(), notEqual()`.
