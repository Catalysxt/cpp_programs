A **queue** is similar to a **stack**. However, in a queue, we can enter values from the rear. We can inspect
and remove values at the front. The queue is implemented as a linked list. The Queue object holds a pointer to the first and last position unlike the **stack** structure.

We implement it with a linked list.

We can use the `Cell` class. We add a `setNext` method.

The `Queue` object holds a pointer to the first and last position unlike `Stack`.
